Benchmark report moved
======================

The benchmark report is now part of :doc:`benchmark_gallery`.

Use the gallery to browse plots and tables, or generate the standalone HTML report with:

.. code-block:: bash

   python benchmarks/make_report.py --outdir results/report
