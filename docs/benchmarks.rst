Benchmarks moved
================

The public benchmark entry point is now :doc:`benchmark_gallery`.

The old split between ``Benchmarks`` and ``Benchmark report`` was replaced by a single visual
benchmark gallery with cards, plots, captions, and detailed benchmark pages.
